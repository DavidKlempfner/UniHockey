@model UniHockey.Models.Tournament

@using (Html.BeginForm("Index", "Game"))
{
    @Html.LabelFor(m => m.Team1Id)
    @Html.DropDownListFor(m => m.Team1Id, Model.Teams)
    @Html.LabelFor(m => m.Team2Id)
    @Html.DropDownListFor(m => m.Team2Id, Model.Teams)
    <input type="submit" value="Create game" />
}